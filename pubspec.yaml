name: locy
description: Easies and fastest way to localize your Flutter App
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  yaml: ^3.1.2
  intl: ^0.18.0
  dart_style: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  dart_style: ^2.3.1
  analyzer: ^5.13.0
  path: ^1.8.3

flutter:
  assets:
    - assets/
